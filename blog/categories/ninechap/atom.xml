<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: ninechap | Shuatiblog.com]]></title>
  <link href="www.shuatiblog.com/blog/categories/ninechap/atom.xml" rel="self"/>
  <link href="www.shuatiblog.com/"/>
  <updated>2015-06-10T10:29:06+08:00</updated>
  <id>www.shuatiblog.com/</id>
  <author>
    <name><![CDATA[CodeMonkey]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[[NineChap 10] Additional Questions]]></title>
    <link href="www.shuatiblog.com/blog/2014/07/07/NineChap-additional/"/>
    <updated>2014-07-07T00:00:00+08:00</updated>
    <id>www.shuatiblog.com/blog/2014/07/07/NineChap-additional</id>
    <content type="html"><![CDATA[The following questions does not appear in NineChap, but they all worth reading. 

### Question List 

1. __[Longest Substring Without Repeating Characters ](/blog/2014/04/27/longest-substring-without-repeating/)__

1. __[Minimum Window Substring ](/blog/2014/05/21/Minimum-Window-Substring/)__ - very difficult

1. __[Scramble String ](/blog/2014/05/23/Scramble-String/)__

1. __[Recover Binary Search Tree ](/blog/2014/05/25/Recover-Binary-Search-Tree/)__

1. __[Median of Two Sorted Arrays ](/blog/2014/04/26/Median-of-Two-Sorted-Arrays/)__ - difficult

1. __[Sudoku Solver ](/blog/2014/05/14/Sudoku-Solver/)__ - difficult

1. __[Word Search ](/blog/2014/05/21/Word-Search/)__

1. __[Simplify Path](/blog/2014/05/21/Simplify-Path/)__

1. __[Regular Expression Matching](/blog/2014/04/29/Regular-Expression-Matching/)__ - very difficult

1. __[Wildcard Matching](/blog/2014/05/15/Wildcard-Matching/)__ - difficult
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[[NineChap 9] Big Date, System Design and Resume (`)]]></title>
    <link href="www.shuatiblog.com/blog/2014/06/30/NineChap-Big-date-system-design-resume/"/>
    <updated>2014-06-30T00:00:00+08:00</updated>
    <id>www.shuatiblog.com/blog/2014/06/30/NineChap-Big-date-system-design-resume</id>
    <content type="html"><![CDATA[## Resume

1. Do not write anything unrelated to CS. 
1. Do not write too long - 1 or 2 pages are fine. Senior engineer 3 pages. 
1. Do not write low GPA
1. Never ever write "proficient in anything"

## Big Data

Most classic question is "Frequent items" (refer to July's blog). 

### Find top k hot queries in a daily access log of Google. 

Variation:

1. k = 1 vs k = 100000 - majority numbers
1. low RAM vs sufficient RAM
1. single machine vs multiple machines
1. accurate vs inaccurate

Sufficient RAM

1. HashTable + Heap (min-heap)
1. Time O(n * logk), Space O(n)

Low RAM

1. Split into 1000 (i.e. LOG/M) files by hash(query) % 1000
1. Using HashTable + Heap to get top k for each files
1. Collect 1000 top k queries and get global top k
1. This method requires a lot of disk access and r/w, still slow. 

Inaccurate (reduce memory from O(n) to O(k))

1. Hash Count (only need to know this one)
    Limit the size of HashMap. The bigger the RAM, the more accurate is the result. 
1. Space Saving
1. Lossy Counting
1. Sticky Sampling
1. Count Sketch

Bloom Filter

1. Regular bloom filter - use 4 线性无关 formula
1. Counting bloom filter - support delete
1. Better DS than HashMap, but can loose some accuracy

Trie

Bitmap

Find all unique queries - use bigmap to store 3 types of states

## System Design

### Design a short url system

1. Cache 

> to store hot urls

1. Load Balance 

> Too many click in short time

1. Storage balance

> Hash value of an url and then store in 
individual machine
>
> Expansibility?

1. Consistent Hash

> Node, can increase # of machines to store information

> Migration process

1. Router

> check which machine response my query
>
> light-weight calculations
>
> what is router is down?

1. Locale

> url frequently access by China, then put the url storage in Beijing

### Need-to-know Design patterns

1. Singleton
1. Factory
1. Master-slave (esp. for relational DB)

[MapReduce: Simplified Data Processing on Large Clusters](http://static.googleusercontent.com/media/research.google.com/en//archive/mapreduce-osdi04.pdf)

[The Google File System](http://static.googleusercontent.com/media/research.google.com/en//archive/gfs-sosp2003.pdf)

[BigTable: A Distributed Storage System for Structured Data](http://static.googleusercontent.com/media/research.google.com/en//archive/bigtable-osdi06.pdf)
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[[NineChap 8] High Frequency Questions]]></title>
    <link href="www.shuatiblog.com/blog/2014/06/28/NineChap-High-frequency/"/>
    <updated>2014-06-28T00:00:00+08:00</updated>
    <id>www.shuatiblog.com/blog/2014/06/28/NineChap-High-frequency</id>
    <content type="html"><![CDATA[## Number & Bit questions

1. __[Single Number](/blog/2014/06/01/Single-Number/)__
1. __[Single Number II](/blog/2014/06/01/Single-Number-II/)__
1. __[Single Number III](/blog/2014/06/28/Single-Number-III/)__
1. __[Single Number IV](/blog/2014/06/28/Single-Number-IV/)__
1. __[Majority Number](/blog/2014/06/28/Majority-Number/)__
1. __[Majority Number II](/blog/2014/06/28/Majority-Number-II/)__
1. __[Majority Number III](/blog/2014/06/28/Majority-Number-III/)__

## Subarray questions

Always using the idea of 前缀和. 

1. __[Best Time to Buy and Sell Stock](/blog/2014/05/28/Best-Time-to-Buy-and-Sell-Stock/)__ - 贪心法
1. __[Best Time to Buy and Sell Stock II](/blog/2014/05/28/Best-Time-to-Buy-and-Sell-Stock-II/)__
1. __[Best Time to Buy and Sell Stock III](/blog/2014/05/28/Best-Time-to-Buy-and-Sell-Stock-III/)__
1. __[Maximum Subarray](/blog/2014/05/20/Maximum-Subarray/)__
1. __[Minimum Subarray ](/blog/2014/06/28/Minimum-subarray/)__
1. __[Maximum Subarray II](/blog/2014/06/28/Maximum-subarray-II/)__
1. __[Subarray with 0 Sum](/blog/2014/07/04/Subarray-with-0-Sum/)__
1. __[Subarray with Particular Sum](/blog/2014/07/04/Subarray-with-Particular-Sum/)__
1. __[Subarray with Sum Closest](/blog/2014/07/04/Subarray-with-Sum-Closest/)__

## N Sum questions

1. __[Two Sum](/blog/2014/04/26/two-sum/)__ - difficult
1. __[3 Sum](/blog/2014/05/02/3Sum/)__
1. __[3 Sum Closest](/blog/2014/05/02/3Sum-Closest/)__
1. __[4 Sum](/blog/2014/05/03/4Sum/)__ - doing a O(n^3) solution is good enough.
1. __k sum questions__ are basically solved with O(n^(k-1)) time. Faster solution is available but too complex. 

## L 家最爱

1. __[Pow(x,n)](/blog/2014/05/15/PowXN/)__
1. __[Sqrt(x)](/blog/2014/05/21/SqrtX/)__
1. __[Trailing Zeros of Factorial](/blog/2014/07/02/Trailing-Zero-of-Factorial/)__
1. __[Check Power of 2](/blog/2014/07/04/Check-Power-of-2/)__

## Additional questions

1. __[Partition Array](/blog/2014/06/28/Partition-array/)__
1. __[Sort Color](/blog/2014/05/21/Sort-Colors/)__

## Code

#### Number questions

__Single Number__

    public int singleNumber(int[] A) {
        int x = 0;
        for (Integer a: A) {
            x = x ^ a;
        }
        return x;
    }

__Single Number II__

Last time, I used an array of size 32 to store count, but it's actually not necessary. 

    public int singleNumber(int[] A) {
        int ans = 0;
        for (int i = 0; i < 32; i++) {
            int count = 0;
            for (Integer a: A) {
                count += ((a >> i) & 1);
            }
            ans |= (count % 3) << i;
        }
        return ans;
    }

#### Subarray questions

__Best Time to Buy and Sell Stock__

    public int maxProfit(int[] prices) {
        if (prices == null || prices.length == 0) {
            return 0;
        }
        int min = prices[0];
        int profit = 0;
        for (Integer p: prices) {
            min = Math.min(min, p);
            profit = Math.max(profit, p - min);
        }
        return profit;
    }

__Best Time to Buy and Sell Stock II__

    public int maxProfit(int[] prices) {
        if (prices == null || prices.length == 0) {
            return 0;
        }
        int pre = prices[0];
        int profit = 0;
        for (Integer p: prices) {
            if (p > pre) {
                profit += p - pre;
            }
            pre = p;
        }
        return profit;
    }

__Best Time to Buy and Sell Stock III__

It's important to note the 2nd last line of the code, where we consider the corner case of doing only 1 transaction. 

It's always best to list a simple test case and walk it thru before submitting the code. 

    public int maxProfit(int[] prices) {
        if (prices == null || prices.length == 0) {
            return 0;
        }
        int len = prices.length;
        int[] dpLeft = new int[len];
        int leftMin = prices[0];
        for (int i = 1; i < len; i++) {
            dpLeft[i] = Math.max(dpLeft[i - 1], prices[i] - leftMin);
            leftMin = Math.min(leftMin, prices[i]);
        }
        int[] dpRight = new int[len];
        int rightMax = prices[len - 1];
        for (int i = len - 2; i >= 0; i--) {
            dpRight[i] = Math.max(dpRight[i + 1], rightMax - prices[i]);
            rightMax = Math.max(rightMax, prices[i]);
        }
        // now iterate the 2 DP array and find out the largest possible profit
        int profit = 0;
        for (int i = 0; i < len - 1; i++) {
            profit = Math.max(profit, dpLeft[i] + dpRight[i + 1]);
        }
        int oneTransaction = Math.max(dpLeft[len - 1], dpRight[0]);
        return Math.max(profit, oneTransaction);
    }

__Maximum Subarray__

    public int maxSubArray(int[] A) {
        if (A == null || A.length == 0) {
            return 0;
        }
        int max = Integer.MIN_VALUE;
        int pre = 0;
        // the largest sum ending at previous position in the array
        for (Integer a: A) {
            max = Math.max(max, pre + a);
            pre = Math.max(0, pre + a);
        }
        return max;
    }

#### 3Sum questions

__Two Sum__

This solution is O(nlgn) time. 

Alternatively, we can use HashMap to solve this problem with O(n) time. 

    public int[] twoSum(int[] numbers, int target) {
        // write your code here
        int[] ans = new int[2];
        if (numbers == null || numbers.length == 0) {
            return ans;
        }
        int len = numbers.length;
        Pair[] pairs = new Pair[len];
        for (int i = 0; i < len; i++) {
            pairs[i] = new Pair(numbers[i], i + 1);
        }
        Arrays.sort(pairs);
        int left = 0;
        int right = len - 1;
        while (left < right) {
            if (pairs[left].num + pairs[right].num == target) {
                ans[0] = pairs[left].index;
                ans[1] = pairs[right].index;
                Arrays.sort(ans);
                break;
            } else if (pairs[left].num + pairs[right].num > target) {
                right--;
            } else {
                left++;
            }
        }
        return ans;
    }
	
    class Pair implements Comparable<Pair> {
        int num;
        int index;
        
        public Pair(int a, int b) {
            num = a;
            index = b;
        }
        
        public int compareTo(Pair another) {
            return this.num - another.num;
        }
    }

__3 Sum__

    public ArrayList<ArrayList<Integer>> threeSum(int[] numbers) {
        ArrayList<ArrayList<Integer>> ans = new ArrayList<ArrayList<Integer>>();
        if (numbers == null || numbers.length == 0) {
            return ans;
        }
        Arrays.sort(numbers);
        int len = numbers.length;
        for (int i = 0; i < len; i++) {
            if (i > 0 && numbers[i - 1] == numbers[i]) {
                continue;
            }
            int left = i + 1;
            int right = len - 1;
            // find 2 numbers that sums to - number[i]
            while (left < right) {
                int diff = numbers[left] + numbers[right] + numbers[i];
                if (diff == 0) {
                    ArrayList<Integer> triplet = new ArrayList<Integer>();
                    triplet.add(numbers[i]);
                    triplet.add(numbers[left]);
                    triplet.add(numbers[right]);
                    ans.add(triplet);
                }
                if (diff <= 0) {
                    left++;
                    while (left < len && numbers[left - 1] == numbers[left]) {
                        left++;
                    }
                }
                if (diff >= 0) {
                    right--;
                    while (right >= 0 && numbers[right + 1] == numbers[right]) {
                        right--;
                    }
                }
            }
        }
        return ans;
    }

__3 Sum Closest__

    public int threeSumClosest(int[] numbers, int target) {
        if (numbers == null || numbers.length == 0) {
            return 0;
        }
        Arrays.sort(numbers);
        int sum = 0;
        int diff = Integer.MAX_VALUE;
        int len = numbers.length;
        for (int i = 0; i < len; i++) {
            int left = i + 1;
            int right = len - 1;
            while (left < right) {
                int triple = numbers[left] + numbers[right] + numbers[i];
                if (triple == target) {
                    return target;
                } else if (triple < target) {
                    left++;
                } else {
                    right--;
                }
                if (Math.abs(target - triple) < diff) {
                    diff = Math.abs(target - triple);
                    sum = triple;
                }
            }
        }
        return sum;
    }

__4 Sum__

    public ArrayList<ArrayList<Integer>> fourSum(int[] numbers, int target) {
        ArrayList<ArrayList<Integer>> ans = new ArrayList<ArrayList<Integer>>();
        if (numbers == null || numbers.length == 0) {
            return ans;
        }
        Arrays.sort(numbers);
        int len = numbers.length;
        for (int i = 0; i < len - 3; i++) {
            if (i > 0 && numbers[i - 1] == numbers[i]) {
                continue;
            }
            for (int j = i + 1; j < len - 2; j++) {
                if (j > i + 1 && numbers[j - 1] == numbers[j]) {
                    continue;
                }
                int left = j + 1;
                int right = len - 1;
                while (left < right) {
                    int diff = numbers[left] + numbers[right] + numbers[i] + numbers[j] - target;
                    if (diff == 0) {
                        ArrayList<Integer> triplet = new ArrayList<Integer>();
                        triplet.add(numbers[i]);
                        triplet.add(numbers[j]);
                        triplet.add(numbers[left]);
                        triplet.add(numbers[right]);
                        ans.add(triplet);
                    }
                    if (diff <= 0) {
                        left++;
                        while (left < len && numbers[left - 1] == numbers[left]) {
                            left++;
                        }
                    }
                    if (diff >= 0) {
                        right--;
                        while (right >= 0 && numbers[right + 1] == numbers[right]) {
                            right--;
                        }
                    }
                }
            }
        }
        return ans;
    }

#### L 家最爱

__Pow(x,n)__

It's important to note that in Line 16, wrting 'while (pow * 2 <= y)' would not work (because of overflow). It took me a long time to find this bug. 

    public double pow(double x, int n) {
        if (n < 0) {
            return 1.0 / helper (x, 0 - n);
        } else {
            return helper(x, n);
        }
    }
    
    private double helper(double x, int y) {
        if (y == 0) {
            return 1.0;
        }
        int pow = 1;
        double num = x;
        while (pow <= y / 2) {
            num *= num;
            pow <<= 1;
        }
        return num * helper(x, y - pow);
    }

__Sqrt(x)__

Note that in Line 8, we must declare left and right as 'long', not 'int', otherwise there will be overflow problems. It took me a long time to find this bug. 

    public int sqrt(int x) {
        if (x < 0) {
            return -1;
        } else if (x < 2) {
            return x;
        }
        long left = 1;
        long right = x;
        while (left + 1 < right) {
            long mid = left + (right - left) / 2;
            if (mid * mid < x) {
                left = mid;
            } else if (mid * mid > x) {
                right = mid;
            } else {
                return (int) mid;
            }
        }
        return (int) left;
    }

#### Additional

__Sort Color__

    public void sortColors(int[] A) {
        if (A == null || A.length == 0) {
			return;
		}
		int len = A.length;
		partition(A, 0, len - 1, 0);
		int p = 0;
		while (p < len && A[p] == 0) {
			p++;
		}
		partition(A, p, len - 1, 1);
    }
	
	private void partition(int[] A, int start, int end, int target) {
		// find the target and put it on the left side of the array
		while (start < end) {
			while (start < A.length && A[start] == target) {
				start++;
			}
			while (end >= 0 && A[end] != target) {
				end--;
			}
			if (start > end) {
				break;
			} else {
				int temp = A[start];
				A[start] = A[end];
				A[end] = temp;
			}
		}
	}
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[[NineChap 7] Data Structure]]></title>
    <link href="www.shuatiblog.com/blog/2014/06/28/NineChap-Data-structure/"/>
    <updated>2014-06-28T00:00:00+08:00</updated>
    <id>www.shuatiblog.com/blog/2014/06/28/NineChap-Data-structure</id>
    <content type="html"><![CDATA[## Data Structure

Data structure is a way to manage data. It provides some methods to handle data stream. For example, DB is a DS.

### Stack and Queue

1. __[Min-stack](/blog/2014/07/01/Min-Stack/)__

1. __[Implement a queue by two stacks](/blog/2014/07/01/Implement-queue-with-stack/)__

1. __[Largest Rectangle in histogram](/blog/2014/05/23/Largest-Rectangle-in-Histogram/)__

### Hash

#### Hash function

1. MD5
1. Magic number 33 (__PHP hash function [DJBX33A](http://events.ccc.de/congress/2011/Fahrplan/attachments/2007_28C3_Effective_DoS_on_web_application_platforms.pdf)__)

Magic Number: 

    int hashfunc(String key) {
        int sum = 0;
        for (int i = 0; i < key.length(); i++) {
            sum = sum * 33 + (int)(key.charAt(i));
            sum = sum % HASH_TABLE_SIZE;
        }
        return sum
    }
    
#### Collision

1. __Close hashing (also called Open addressing)__

    Resolves conflict by probing, or searching through alternate locations in the array
        
    Suck scheme may cause the lookup cost to skyrocket. Not good to use.

1. __Open hashing__

    Keys stored in linked lists attached to cells of the hash table.
    
    Practically, hash size set around 10 times the size of data
    
    Used by Java and most other languages. 

#### Rehashing

1. Memcached is a general-purpose distributed memory caching system. One of its bottleneck is rehashing, which locks down the entire hash.

1. Dynamic resizing (normally size * 2) and copy all elements into the new hash. 

1. Extremely slow process, we should try to avoid it by setting a large enough initial size. 

#### Hash questions:

1. __[Implement a hashmap](/blog/2014/07/01/Implement-Hashmap/)__

1. __[HashMap vs Hashtable vs HashSet](/blog/2014/07/01/Hashmap-Hashtable-Hashset/)__

1. __[LRU Cache](/blog/2014/06/03/LRU-Cache/)__

1. __[Longest consecutive sequence](/blog/2014/05/29/Longest-Consecutive-Sequence/)__

### Heap

1. Child is always larger than parent
1. Heap is not a sorted structure, but it's partially ordered
1. Heap is always balanced 

Heap is better than array because average of 3 operations is O(logn), but array is O(n). 

>Add O(log N)
>
>Remove O(log N)
>
>Min/Max O(1)

#### Heap implementation: 

1. Low Level: dynamic array, not list

1. Internal Method: Shiftup, Shiftdown operations

1. A heap is a complete binary tree (最优二叉树) __represented by an array__

1. When removing element from heap, we actually uses HashMap to find that element.

> Heaps are usually implemented in an array, and do not require pointers between elements.
>
> Full and almost full binary heaps may be represented in a very space-efficient way using an array alone. The first element will contain the root. The next two elements of the array contain its children. The next four contain the four children of the two child nodes, etc. 
>
> Thus the children of the node at position n would be at positions 2n+1 and 2n+2. 

So to summarize: 

1. elems[1] - root, also the minimum elem in elems.
1. elems[i]: left child is elems[i*2], right child is elems[i*2+1]

Implementation code: 

    Add:
        Push back to elems; size ++; Siftup;
    Remove:
        Replace the elem to be removed with the last elem; 
        size --; 
        Siftup and Siftdown.

#### Heap questions:

1. __[Median in a stream of integers](/blog/2014/07/01/Median-in-stream-of-integers/)__

1. __[The Skyline Problem](/blog/2014/07/01/The-Skyline-Problem/)__

### Interval Tree

Easily find the max/min value in an interval. 2 example questions are: 

1. Find min/max/sum in an interval
1. 最长的连续1

## Code

__Largest Rectangle in histogram__

    public int largestRectangleArea(int[] height) {
        if (height == null || height.length == 0) {
            return 0;
        }
        Stack<Integer> stack = new Stack<Integer>();
        stack.add(0);
        int len = height.length;
        int area = 0;
        for (int i = 1; i <= len; i++) {
            int h = i == len ? 0 : height[i];
            // pop a element and calculate its max area
            // pop until the top element is smaller than h, then push h
            while (!stack.isEmpty() && h < height[stack.peek()]) {
                int pos = stack.pop();
                int width = stack.isEmpty() ? i : i - stack.peek() - 1;
                area = Math.max(area, height[pos] * width);
            }
            stack.push(i);
        }
        return area;
    }

__LRU Cache__

I posted code in the new post. 

__Longest consecutive sequence__

    public int longestConsecutive(int[] num) {
        if (num == null || num.length == 0) {
            return 0;
        }
        HashSet<Integer> set = new HashSet<Integer>();
        for (Integer i: num) {
            set.add(i);
        }
        int longest = 0;
        for (Integer i: num) {
            if (!set.contains(i)) {
                continue;
            }
            int left = i - 1;
            while (set.contains(left)) {
                set.remove(left--);
            }
            int right = i + 1;
            while (set.contains(right)) {
                set.remove(right++);
            }
            longest = Math.max(longest, right - left - 1);
            set.remove(i);
        }
        return longest;
    }
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[[NineChap 6] Graph and Search]]></title>
    <link href="www.shuatiblog.com/blog/2014/06/26/NineChap-Graph/"/>
    <updated>2014-06-26T00:00:00+08:00</updated>
    <id>www.shuatiblog.com/blog/2014/06/26/NineChap-Graph</id>
    <content type="html"><![CDATA[## Graph

For graph, there are only 2 high-frequency questions, which is 'clone graph' and 'topology sorting'. 

### Question list

1. __[Clone Graph](/blog/2014/05/30/Clone-Graph/)__ - difficult

1. __[Topology Sorting](/blog/2014/06/27/Topology-sorting/)__

## Search

Search have either DFS or BFS. 

First, we will cover permutations and combinations using DFS. In this section we solve the famous N-queens question. 

Then, there's a few BFS questions. Graph traversal is BFS, and Word ladder is also a classic BFS question. 

### Question list

1. __[Subsets](/blog/2014/05/22/Subsets/)__

1. __[Subsets II](/blog/2014/05/22/Subsets-II/)__
    
    difficult

1. __[Permutations](/blog/2014/05/14/Permutations/)__

1. __[Permutations II](/blog/2014/05/14/Permutations-II/)__ - difficult

1. __[N-Queens](/blog/2014/05/16/N-Queens/)__

    how to use hashmap (and some space) to make it faster? 3 hashmaps to store the row, the (x,y) diff and sum. This will make isValid() method O(1). 

1. __[N-Queens II](/blog/2014/05/16/N-Queens-II/)__

1. __[Next Permutation](/blog/2014/05/12/Next-Permutation/)__

1. __[Previous Permutation](/blog/2014/06/27/Previous-Permutation/)__

1. __[Palindrome Partitioning](/blog/2014/05/29/Palindrome-Partitioning/)__

1. __[Palindrome Partitioning II](/blog/2014/05/30/Palindrome-Partitioning-II/)__

1. __[Combination Sum](/blog/2014/05/13/Combination-Sum/)__

1. __[Combination Sum II](/blog/2014/05/13/Combination-Sum/)__

1. __[Word Ladder](/blog/2014/05/29/Word-Ladder/)__

1. __[Word Ladder II](/blog/2014/05/30/Word-Ladder-II/)__

### Additional questions

1. __[Restore IP Addresses](/blog/2014/05/24/Restore-IP-Addresses/)__

1. __[Combinations](/blog/2014/05/22/Combinations/)__

1. __[Letter Combinations of a Phone Number](/blog/2014/05/02/Letter-Combinations-of-a-Phone-Number/)__

1. __[Permutation Sequence](/blog/2014/05/19/Permutation-Sequence/)__

## Code 

### Graph

__Clone Graph__

    public UndirectedGraphNode cloneGraph(UndirectedGraphNode node) {
        if (node == null) return null;
        HashMap<UndirectedGraphNode, UndirectedGraphNode> map = 
                new HashMap<UndirectedGraphNode, UndirectedGraphNode>();
        Queue<UndirectedGraphNode> queue = new LinkedList<UndirectedGraphNode>();
        map.put(node, new UndirectedGraphNode(node.label));
        queue.add(node);
        while (!queue.isEmpty()) {
            UndirectedGraphNode cur = queue.remove();
            UndirectedGraphNode copy = map.get(cur);
            // here the 'copy' must exist. why? because all neighbors 
            // has been added to the map when they're pushed to queue.
            // so 'cur' must have a corresponding copy in the hashmap. 
            for (UndirectedGraphNode neib: cur.neighbors) {
                if (!map.containsKey(neib)) {
                    queue.add(neib);
                    map.put(neib, new UndirectedGraphNode(neib.label));
                }
                copy.neighbors.add(map.get(neib));
            }
        }
        return map.get(node);
    }

__Topology Sorting__

A new post is written for it. 

### Search

__Subsets__

    public List<List<Integer>> subsets(int[] num) {
        List<List<Integer>> ans = new LinkedList<List<Integer>>();
        if (num == null || num.length == 0) {
            return ans;
        }
        Arrays.sort(num);
        helper(ans, new LinkedList<Integer>(), num, 0);
        return ans;
    }
    
    private void helper(List<List<Integer>> ans, List<Integer> path, int[] num, int pos) {
        ans.add(new LinkedList<Integer>(path));
        for (int i = pos; i < num.length; i++) {
            path.add(num[i]);
            helper(ans, path, num, i + 1);
            path.remove(path.size() - 1);
        }
    }

__Subsets II__

    public List<List<Integer>> subsetsWithDup(int[] num) {
        List<List<Integer>> ans = new LinkedList<List<Integer>>();
        if (num == null || num.length == 0) {
            return ans;
        }
        Arrays.sort(num);
        helper(ans, new LinkedList<Integer>(), num, 0);
        return ans;
    }
    
    private void helper(List<List<Integer>> ans, List<Integer> path, int[] num, int pos) {
        ans.add(new LinkedList<Integer>(path));
        for (int i = pos; i < num.length; i++) {
            if (i > pos && num[i - 1] == num[i]) {
                continue;
            }
            path.add(num[i]);
            helper(ans, path, num, i + 1);
            path.remove(path.size() - 1);
        }
    }

__Permutations__

    public List<List<Integer>> permute(int[] num) {
        List<List<Integer>> ans = new LinkedList<List<Integer>>();
        if (num == null || num.length == 0) {
            return ans;
        }
        helper(ans, new LinkedList<Integer>(), num);
        return ans;
    }
    
    private void helper(List<List<Integer>> ans, List<Integer> path, int[] num){
        if (path.size() == num.length) {
            ans.add(new LinkedList<Integer>(path));
        }
        for (int i = 0; i < num.length; i++) {
            if (path.contains(num[i])) {
                continue;
            }
            path.add(num[i]);
            helper(ans, path, num);
            path.remove(path.size() - 1);
        }
    }

__Permutations II__

    public List<List<Integer>> permuteUnique(int[] num) {
        List<List<Integer>> ans = new LinkedList<List<Integer>>();
        if (num == null || num.length == 0) {
            return ans;
        }
        Arrays.sort(num);
        helper(ans, new LinkedList<Integer>(), num, new int[num.length]);
        return ans;
    }
    
    private void helper(List<List<Integer>> ans, List<Integer> path, int[] num, int[] visited){
        if (path.size() == num.length) {
            ans.add(new LinkedList<Integer>(path));
            return;
        }
        for (int i = 0; i < num.length; i++) {
            if (visited[i] == 1) {
                continue;
            }
            if (i > 0 && visited[i - 1] == 1 && visited[i] == 0 && num[i - 1] == num[i]) {
                // if current number is same as previous, then don't visit current
                continue;
            }
            path.add(num[i]);
            visited[i] = 1;
            
            helper(ans, path, num, visited);
            
            path.remove(path.size() - 1);
            visited[i] = 0;
        }
    }

__N-Queens__

一次通关！高兴。

    public List<String[]> solveNQueens(int n) {
        List<String[]> ans = new LinkedList<String[]>();
		if (n <= 0) {
			return ans;
		}
		helper(ans, new int[n], n, 0);
		return ans;
    }
	
	private void helper(List<String[]> ans, int[] path, int n, int pos) {
		if (pos >= n) {
			ans.add(convert(path, n));
			return;
		}
		for (int i = 0; i < n; i++) {
			path[pos] = i;
			if (!isValid(path, pos)) {
				continue;
			}
			helper(ans, path, n, pos + 1);
		}
	}
	
	private String[] convert(int[] path, int n) {
		String[] ans = new String[n];
		for (int j = 0; j < n; j++) {
			ans[j] = "";
			for (int i = 0; i < n; i++) {
				ans[j] += (j == path[i]) ? 'Q' : '.';
			}
		}
		return ans;
	}
	
	private boolean isValid(int[] path, int pos) {
		for (int i = 0; i < pos; i++) {
			// check path[i] and path[pos]
			if (path[i] == path[pos]) {
				return false;
			}
			if (path[i] - path[pos] == pos - i) {
				return false;
			}
			if (path[pos] - path[i] == pos - i) {
				return false;
			}
		}
		return true;
	}

__N-Queens II__

	int total;
    
	public int totalNQueens(int n) {
		if (n <= 0) {
			return 0;
		}
		helper(new int[n], n, 0);
		return total;
    }
	
	private void helper(int[] path, int n, int pos) {
		if (pos >= n) {
			total++;
			return;
		}
		for (int i = 0; i < n; i++) {
			path[pos] = i;
			if (!isValid(path, pos)) {
				continue;
			}
			helper(path, n, pos + 1);
		}
	}
	
	private boolean isValid(int[] path, int pos) {
		for (int i = 0; i < pos; i++) {
			// check path[i] and path[pos]
			if (path[i] == path[pos]) {
				return false;
			}
			if (path[i] - path[pos] == pos - i) {
				return false;
			}
			if (path[pos] - path[i] == pos - i) {
				return false;
			}
		}
		return true;
	}

__Next Permutation__

    public void nextPermutation(int[] num) {
        if (num == null || num.length <= 1) {
			return;
		}
		int len = num.length;
		int p = len - 2;
		while (p >= 0 && num[p] >= num[p + 1]) {
			p--;
		}
		if (p < 0) {
			Arrays.sort(num);
		} else {
			int k = len - 1;
			while (k >= 0 && num[k] <= num[p]) {
				k--;
			}
			swap(num, p, k);
			reverse(num, p + 1, len - 1);
		}
    }
	
	private void swap(int[] num, int p, int k) {
		num[p] = num[p] + num[k];
		num[k] = num[p] - num[k];
		num[p] = num[p] - num[k];
	}
	
	private void reverse(int[] num, int s, int d) {
		while (s < d) {
			swap(num, s++, d--);
		}
	}

__Previous Permutation__

Plz look at the new post. 

__Palindrome Partitioning__

一次通关 again！very 高兴。

    public ArrayList<ArrayList<String>> partition(String s) {
		ArrayList<ArrayList<String>> ans = new ArrayList<ArrayList<String>>();
		if (s == null || s.length() == 0) {
			return ans;
		}
		boolean[][] map = palinMap(s);
		helper(ans, new ArrayList<String>(), s, map, 0);
		return ans;
    }
	
	private void helper(ArrayList<ArrayList<String>> ans, ArrayList<String> path, String s, boolean[][] map, int pos) {
		int len = s.length();
		if (pos == len) {
			ans.add(new ArrayList<String>(path));
			return;
		}
		for (int i = pos; i < len; i++) {
			if (!map[pos][i]) {
				continue;
			}
			path.add(s.substring(pos, i + 1));
			helper(ans, path, s, map, i + 1);
			path.remove(path.size() - 1);
		}
	}
	
	private boolean[][] palinMap(String s) {
		int len = s.length();
		boolean[][] map = new boolean[len][len];
		for (int i = len - 1; i >= 0; i--) {
			for (int j = 0; j < len; j++) {
				if (i > j) {
					continue;
				} else if (i == j) {
					map[i][j] = true;
				} else if (j - i == 1) {
					map[i][j] = s.charAt(i) == s.charAt(j);
				} else {
					map[i][j] = map[i + 1][j - 1] & 
							s.charAt(i) == s.charAt(j);
				}
			}
		}
		return map;
	}

__Palindrome Partitioning II__

This is DP, not Graph & Search. 

__Combination Sum__

    public ArrayList<ArrayList<Integer>> combinationSum(int[] candidates, int target) {
		ArrayList<ArrayList<Integer>> ans = new ArrayList<ArrayList<Integer>>();
		if (candidates == null || candidates.length == 0) {
			return ans;
		}
		Arrays.sort(candidates);
		helper(ans, new ArrayList<Integer>(), candidates, target, 0);
		return ans;
	}
	
	private void helper(ArrayList<ArrayList<Integer>> ans, ArrayList<Integer> path, 
			int[] candidates, int target, int pos) {
		if (target == 0) {
			ans.add(new ArrayList<Integer>(path));
			return;
		} else if (target < 0) {
			return;
		}
		for (int i = pos; i < candidates.length; i++) {
			if (target < candidates[i]) {
				continue;
			}
			path.add(candidates[i]);
			helper(ans, path, candidates, target - candidates[i], i);
			path.remove(path.size() - 1);
		}
	}

__Combination Sum II__

    public ArrayList<ArrayList<Integer>> combinationSum2(int[] candidates, int target) {
		ArrayList<ArrayList<Integer>> ans = new ArrayList<ArrayList<Integer>>();
		if (candidates == null || candidates.length == 0) {
			return ans;
		}
		Arrays.sort(candidates);
		helper(ans, new ArrayList<Integer>(), candidates, target, 0);
		return ans;
	}
	
	private void helper(ArrayList<ArrayList<Integer>> ans, ArrayList<Integer> path, 
			int[] candidates, int target, int pos) {
		if (target == 0) {
			ans.add(new ArrayList<Integer>(path));
			return;
		} else if (target < 0) {
			return;
		}
		for (int i = pos; i < candidates.length; i++) {
			if (target < candidates[i]) {
				continue;
			}
			if (i > pos && candidates[i - 1] == candidates[i]) {
				continue;
			}
			path.add(candidates[i]);
			helper(ans, path, candidates, target - candidates[i], i + 1);
			path.remove(path.size() - 1);
		}
	}

__Word Ladder__

Note that this is a __BFS question, not DFS__. I made it wrong and it took me a long time. 

    public int ladderLength(String start, String end, Set<String> dict) {
		Queue<String> queue = new LinkedList<String>();
		queue.add(start);
		int length = 1;
		
		while (!queue.isEmpty()) {
		    int currentSize = queue.size();
			for (int k = 0; k < currentSize; k++) {
				String word = queue.remove();
				// insert all adjacent strings of word
				if (word.equals(end)) {
					return length;
				}
				for (int i = 0; i < word.length(); i++) {
					char[] letters = word.toCharArray();
					char originalLetter = letters[i];
					for (char c = 'a'; c <= 'z'; c++) {
						if (c == originalLetter) continue;
						letters[i] = c;
						String newLetters = String.valueOf(letters);
						if (dict.contains(newLetters)) {
							queue.add(newLetters);
							dict.remove(newLetters);
						}
					}
					letters[i] = originalLetter;
				}
			}
			length++;
		}
		return 0;
	}

__Word Ladder II__

unsolvable

### Additional questions

__Restore IP Addresses__

    public List<String> restoreIpAddresses(String s) {
        List<String> ans = new ArrayList<String>();
        if (s == null || s.length() == 0) {
            return ans;
        }
        helper(ans, new ArrayList<String>(), s, 0);
        return ans;
    }
    
    private void helper(List<String> ans, List<String> path, String s, int pos) {
        if (path.size() == 4) {
            if (pos == s.length()) {
                ans.add(convert(path));
            }
            return;
        }
        for (int i = pos + 1; i <= s.length() && i <= pos + 3; i++) {
            String nextNum = s.substring(pos, i);
            if (!isValid(nextNum)) {
                continue;
            }
            path.add(nextNum);
            helper(ans, path, s, i);
            path.remove(path.size() - 1);
        }
    }
    
    private boolean isValid(String str) {
        if (str.length() == 1) {
            return true;
        } else if (str.charAt(0) == '0') {
            return false;
        } else {
            int num = Integer.parseInt(str);
            return 0 <= num && num <= 255;
        }
    }
    
    private String convert(List<String> path) {
        String str = "";
        for (String s: path) {
            str += "." + s;
        }
        return str.substring(1);
    }

__Combinations__

skip

__Letter Combinations of a Phone Number__

skip

__Permutation Sequence__

    public String getPermutation(int n, int k) {
        List<Integer> list = new ArrayList<Integer>();
		int fact = 1;
		for (int i = 1; i <= n; i++) {
			list.add(i);
			fact *= i;
		}
		String ans = "";
		for (int i = n; i > 0; i--) {
			fact = fact / i;
			int rank = (k - 1) / fact;
			k = (k - 1) % fact + 1;
			
			int curNum = list.remove(rank);
			ans += String.valueOf(curNum);
		}
		return ans;
    }

## Conclusion

#### DFS (O(2^n), O(n!))

1. Find all possible solutions
2. Permutations / Subsets

#### BFS (O(m), O(n))

1. Graph traversal
2. Find shortest path in a simple graph

__Two most canonical BFS questions__: 

1. Graph traversal and toposort
2. Word Ladder
]]></content>
  </entry>
  
</feed>
